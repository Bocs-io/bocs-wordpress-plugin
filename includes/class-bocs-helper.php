/**
 * IMPORTANT DEVELOPMENT NOTES:
 * 
 * 1. ALWAYS update cache bust versions when modifying any JS or CSS files.
 *    Use bocs_get_cache_bust_version() with the current date in YYYYMMDD.X format
 *    where X is the version number for that day.
 * 
 * 2. ALWAYS verify the current date with a system command (date "+%Y%m%d")
 *    rather than assuming the current date.
 * 
 * 3. Both CSS and JS files need independent cache busting when modified.
 */

// ... existing code ... 